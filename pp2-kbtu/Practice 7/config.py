# Task 1
def config(host="localhost", database="phonebook_db", user="meirkhanbaidulla", password="student_pass"):
    return {
        "host": host,
        "database": database,
        "user": user,
        "password": password
    }